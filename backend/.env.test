# Server Configuration
PORT=3001
NODE_ENV=test

# MongoDB Configuration (will be overridden by test setup)
MONGODB_URI=mongodb://localhost:27017/sushi-test

# JWT Configuration
JWT_SECRET=test_jwt_secret_key
JWT_EXPIRES_IN=1h
JWT_COOKIE_EXPIRES_IN=1

# AWS S3 Configuration (mock values for testing)
AWS_ACCESS_KEY_ID=test_access_key
AWS_SECRET_ACCESS_KEY=test_secret_key
AWS_REGION=test-region
AWS_BUCKET_NAME=test-bucket

# Stripe Configuration (mock values for testing)
STRIPE_SECRET_KEY=test_stripe_secret_key
STRIPE_WEBHOOK_SECRET=test_stripe_webhook_secret

# Firebase Configuration (mock values for testing)
FIREBASE_PROJECT_ID=test-project
FIREBASE_PRIVATE_KEY=test_private_key
FIREBASE_CLIENT_EMAIL=test@test.com

# Redis Configuration (mock values for testing)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=test_redis_password

# Email Configuration (mock values for testing)
SMTP_HOST=test.smtp.com
SMTP_PORT=587
SMTP_USER=test_user
SMTP_PASS=test_password
EMAIL_FROM=test@sushi.com

# Rate Limiting (relaxed for testing)
RATE_LIMIT_WINDOW_MS=1000
RATE_LIMIT_MAX_REQUESTS=1000

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000 